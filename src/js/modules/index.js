
// Toggle the hover class from an element on mouse-enter and mouse-leave
import(`./_mouse-hover-state.js`).then(module => module.init());

// initialise bootstrap tooltips
import(`./_bs-tooltips.js`).then(module => module.init());

// Project code for Embla carousels
import(`./_embla-carousel.js`).then(module => module.init());

// Globe from Cobe
import(`./_cobe-globe.js`).then(module => module.init());


import(`./_image-slider.js`).then(module => module.init());